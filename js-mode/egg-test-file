# -*- mode: snippet -*-
# name: egg-test-file
# key: etf
# group: egg
# --
/**
 * @fileOverview ${1:file}测试文件
 * @name `(file-name-nondirectory buffer-file-name)`
 * @author kiba.x.zhao <xiafeng@aegleanalytica.io>
 * @license MIT
 */
'use strict';

const { app, assert } = require('egg-mock/bootstrap');
const sinon = require('sinon');

describe('test/${2:path}/`(file-name-nondirectory buffer-file-name)`', () => {

  describe('${3:function}', () => {

    it('${4:success}', async () => {
      $0
      const res = await ${5:function}(${6:args});
      return res;

    });

  });

});
